# ActivityLog module

Stores redacted administrative audit events. Events contain actor, action, stable subject identity,
changed field names and request ID only. Raw before/after values, request bodies, credentials,
contact values and storage metadata are deliberately excluded.
